# مستر پرامپت جامع بازتولید پلتفرم هوشمند انرژی کاپ (KAP Energy Platform)
> این سند حاوی **پرامپت اجرایی جامع و مهندسی‌شده (Master Prompt)** برای ساخت صفر تا صد پلتفرم وبسایت «کاپ» توسط دستیار هوش مصنوعی **Antigravity** است. با ارائه این دستور به Antigravity در یک پروژه جدید، کل معماری، ۱۶ صفحه، ۶ مقاله تخصصی، استایل‌های اختصاصی و استقرار ورسل با بالاترین استاندارد بازتولید خواهد شد.

---

```markdown
شما یک Senior Full-Stack Architect و طراح ارشد رابط کاربری (UI/UX) هستید. وظیفه شما طراحی و پیاده‌سازی صفر تا صد وبسایت جامع و بسیار مدرن **پلتفرم هوشمند انرژی کاپ (KAP Energy Intelligence Platform)** به زبان فارسی (راست‌چین RTL) است. این پلتفرم باید فراتر از یک لندینگ ساده بوده و دارای ۱۶ صفحه مجزا و به هم پیوسته، ۶ مقاله علمی-تحلیلی کامل، معماری بصری خیره‌کننده و کدهای کاملاً استاندارد و پرسرعت بر پایه HTML5، Vanilla CSS3 و JavaScript خالص (بدون وابستگی‌های سنگین) باشد.

---

## ۱. مأموریت، هویت و تعریف فناورانه برند کاپ (KAP)
- **نام تجاری:** KAP (کاپ - کارا انرژی پردازان) — *نکته مهم: در تمام متون انگلیسی و فارسی نام برند منحصراً KAP / کاپ است و هرگز نباید CAP نوشته شود.*
- **تعریف فناورانه رسمی:**
  > «پلتفرم فناورانه در حوزه هوش مصنوعی، اینترنت اشیاء (IoT)، تحلیل داده‌های انرژی، مدیریت هوشمند بار و اقتصاد دیجیتال انرژی که هدف آن ایجاد نسل جدیدی از زیرساخت‌های مدیریت و تبادل انرژی الکتریکی می‌باشد. این طرح با رویکردی نوآورانه، فراتر از سامانه‌های متداول مدیریت ساختمان (BMS) و سامانه‌های پایش مصرف انرژی، به دنبال ایجاد بستری هوشمند برای شناسایی، تحلیل، کنترل و در ادامه معامله‌پذیر نمودن ارزش انرژی الکتریکی است.»
- **ارزش‌های کلیدی:**
  ۱. کاهش مصرف در ساعات اوج بار بدون افت رفاه (تا ۱۵٪ الی ۱۸٪)
  ۲. تبدیل صرفه‌جویی به گواهی‌های قابل معامله انرژی و ایجاد نیروگاه مجازی (VPP)
  ۳. پایش بدون نفوذ بارها (NILM) و تحلیل جریان فرکانس‌بالا توسط سنسورهای CT کلمپی
  ۴. مراقبت هوشمند از سالمندان و افراد آسیب‌پذیر مبتنی بر الگوی مصرف برق (بدون دوربین و نقض حریم خصوصی)

---

## ۲. سیستم طراحی بصری و توکن‌های استایل (Design System)
در فایل `style.css` یک سیستم طراحی مدرن، های‌تک، با هویت پایدار و انرژی پیاده‌سازی کنید:
- **تایپوگرافی:** 
  - فارسی: فونت `Vazirmatn` از Google Fonts با وزن‌های ۳۰۰ تا ۹۰۰
  - اعداد و کدهای انگلیسی: `Inter`
- **پالت رنگی مهندسی‌شده:**
  - نارنجی انرژی (Brand Accent): `--orange-500: #FF5722`, `--orange-600: #E64A19`, `--orange-glow: rgba(255, 87, 34, 0.28)`
  - سورمه‌ای عمیق شبکه و هوش مصنوعی: `--navy-900: #0D1B2A`, `--navy-800: #1B2838`, `--navy-700: #1B3A5C`
  - پس‌زمینه‌های تمیز و کارت‌های مدرن: گرادیان‌های لطیف، بردرهای نئونی ملایم (`border: 1.5px solid rgba(255, 87, 34, 0.25)`), گلس‌مورفیسم، سایه‌های عمیق دوبخشی (`box-shadow: 0 24px 64px rgba(13, 27, 42, 0.14)`).
- **جهت‌گیری:** کاملاً راست‌چین (`direction: rtl; font-family: 'Vazirmatn', sans-serif;`)
- **طراحی ریسپانسیو:** پشتیبانی روان در موبایل (زیر ۴۸۰ پیکسل)، تبلت (زیر ۹۹۲ پیکسل) و دسکتاپ عریض (تا ۱۴۴۰ پیکسل).

---

## ۳. هدر و منوی ناوبری یکپارچه (Global Header)
در تمام صفحات یک هدر چسبان (Sticky Header) با آیتم‌های زیر قرار گیرد:
1. **لوگو:** آیکون انرژی هگزاگون با گرادیان نارنجی + نوشته `KAP` و زیرنویس `ENERGY INTELLIGENCE`
2. **منوی اصلی (۷ لینک هماهنگ):**
   - خانه (`index.html`)
   - راهکارها (`solutions.html`)
   - چگونه کار می‌کند (`how-it-works.html`)
   - MVP و پایلوت (`mvp-pilot.html`)
   - دانش و مقالات (`knowledge.html`)
   - درباره کاپ (`about.html`)
   - سوالات متداول (`faq.html`)
3. **دکمه‌های اقدام (CTA):**
   - دکمه تماس با ما (`contact.html`)
   - دکمه نسخه MVP زنده (`https://kap-energy-platform.vercel.app/`)
4. **منوی همبرگری موبایل:** کشویی و با انیمیشن روان.

---

## ۴. ساختار دقیق صفحات اصلی (Core Pages)

### ۱. صفحه اصلی (`index.html`):
- **Hero Section خیره‌کننده:**
  - عنوان اصلی برداری و با وضوح بالا: «حکمرانی انرژی: معماری تاب‌آوری و **پایداری ملی** در عصر ناترازی»
  - بج شناور: «★ حکمرانی انرژی و هوش مصنوعی مدیریت بار»
  - متن توصیفی عمیق پیرامون تبدیل ناترازی به فرصت با هوش مصنوعی و مدل استقرار رایگان کاپ برای کاهش ۱۵ درصدی مصرف و تولید گواهی‌های صرفه‌جویی.
  - ستون تصویری عریض (۵۵٪ از عرض سکشن): دیاگرام سه‌بعدی ایزومتریک AI Energy Management Hub با افکت هاله نوری نارنجی برند کاپ.
  - دکمه‌های ۳گانه: «کاپ چگونه کار می‌کند؟»، «همکاری با کاپ»، «مشاهده نسخه MVP زنده 🚀».
- **سکشن چالش اصلی (Problem Statement):** ۳ کارت تحلیلی (مصرف قابل اصلاح کجاست؟، چه اقدامی مناسب است؟، آیا نتیجه گرفتیم؟).
- **حلقه ارزش ۵ مرحله‌ای محصول:** پایش دقیق → تحلیل داده → شناخت فرصت → اقدام هوشمند → سنجش نتیجه.
- **نمایشگر شاخص‌های کلیدی (Impact Metrics):** کنتورهای ۱۸٪ کاهش پیک، ۴۰٪ کاهش مصرف بیهوده، پایش ۲۴/۷ و گواهی صرفه‌جویی.
- **سکشن راهکارهای بخشی:** خانگی، آموزشی، اداری-تجاری، صنایع سبک، شرکت‌های برق.
- **شبکه مقالات منتخب (۶ کارت مقاله با تصاویر و تگ دسته‌بندی).**
- **دعوت به همکاری نهایی (CTA Banner) و فوتر چندستونه.**

### ۲. صفحه مقالات و پایگاه دانش (`knowledge.html`):
- فیلتر تعاملی دسته‌بندی مقالات (همه، مدیریت بار، اینترنت اشیاء، هوش مصنوعی، سیاست‌گذاری).
- نمایش کارت هر ۶ مقاله با نویسنده، تاریخ، زمان مطالعه و دکمه ادامه مطلب.
- سکشن سناریوهای کاربردی (Case Studies) و معماری امنیت و محرمانگی داده‌های کاپ.

### ۳. صفحه چگونه کار می‌کند (`how-it-works.html`):
- تشریح کامل معماری فنی: از نصب غیرمخرب ترانسفورماتور جریان (CT)، پردازش در لبه (Edge Computing)، الگوریتم‌های هوش مصنوعی تا اتصال به پلتفرم ابری.

### ۴. صفحه MVP و پایلوت (`mvp-pilot.html`):
- ارائه تعریف فناورانه پلتفرم، نتایج به‌دست‌آمده از پایلوت‌های میدانی، جدول مقایسه نسخه فعلی با رودمپ توسعه، و لینک بزرگ نسخه زنده ورسل.

### ۵. صفحه سوالات متداول (`faq.html`):
- آکاردئون‌های تعاملی جاوااسکریپت برای پاسخ به ۷ پرسش کلیدی: تفاوت کاپ با BMS، نحوه محاسبه صرفه‌جویی، هزینه سخت‌افزار، اتصال به شبکه برق، امنیت داده‌ها و...

### ۶. صفحات معرفی و تماس (`about.html`, `contact.html`):
- داستان پیدایش کاپ، چشم‌انداز ملی تاب‌آوری شبکه برق، فرم تماس تعاملی و راه‌های ارتباطی.

### ۷. صفحات تخصصی راهکارها (Solutions Landing Pages):
- `solutions.html` (صفحه فرود تجمیعی)
- `solution-home.html` (راهکار خانگی و مجتمع‌های مسکونی)
- `solution-education.html` (دانشگاه‌ها و مراکز آموزشی)
- `solution-business.html` (ساختمان‌های تجاری و اداری)
- `solution-industry.html` (صنایع کوچک و کارگاه‌های تولیدی)
- `solution-power.html` (شرکت‌های توزیع برق و بهره‌برداران شبکه)

---

## ۵. تولید کامل ۶ مقاله علمی و استراتژیک (Scientific Articles)
هر یک از مقالات زیر باید در یک فایل HTML مجزا با ساختار کامل وبلاگی، هدر مقاله، زمان مطالعه، سرفصل‌های علمی، جداول، نمودارها و باکس جمع‌بندی تولید شوند:

1. **`article-grid.html`:** «کاهش ۱۸٪ مصرف پیک برق در بخش اداری و تجاری با الگوریتم‌های مدیریت هوشمند بار کاپ» (تحلیل بار پایه، شیفت مصرف، سیستم‌های HVAC).
2. **`article-ai.html`:** «نقش الگوریتم‌های یادگیری عمیق در تشخیص زودهنگام خطاهای شبکه و مصارف غیرعادی» (شبکه‌های LSTM، فشرده‌سازی لبه شبکه، جلوگیری از خرابی ترانس‌ها).
3. **`article-ct.html`:** «سنسورهای کلمپی جریان (CT) در مقایسه با کنتورهای هوشمند: راهکار سریع پایش انرژی بدون قطعی برق» (مقایسه هزینه، سرعت استقرار بدون تغییر سیم‌کشی، نمونه‌برداری فرکانس‌بالا).
4. **`article-vpp.html`:** «نیروگاه‌های مجازی (VPP): کلید حل ناترازی برق تابستانه ایران بدون ساخت نیروگاه جدید» (تجمیع بار انعطاف‌پذیر، گواهی صرفه‌جویی، پاسخگویی بار DR).
5. **`article-elderly.html`:** «پایش سلامت و امنیت سالمندان با هوش مصنوعی انرژی: تشخیص رفتار بدون نقض حریم خصوصی و بدون دوربین» (کاربرد پایش غیرتهاجمی NILM، تحلیل الگوی روشن شدن کتری، یخچال و روشنایی، هشدارهای خودکار اورژانسی).
6. **`article-energy-intensity.html`:** «شدت انرژی در ایران و تأثیر ناترازی بر تولید ناخالص داخلی (GDP): راهکارهای دیجیتال برای مهار بحران» (آمار مقایسه‌ای شدت انرژی در ایران در برابر میانگین جهانی، زیان قطعی برق صنایع، مدل‌های پاداش صرفه‌جویی).

---

## ۶. تعاملات کاربری و جاوااسکریپت (`script.js`)
اسکریپت‌ها باید به صورت Modular و کامپکت شامل عملکردهای زیر باشند:
- هدر چسبان پویا (تغییر استایل در اسکرول).
- منوی بازشوی موبایل با کلیک خارج از منو (Click Outside) برای بسته شدن.
- فیلتر تب‌های دسته‌بندی مقالات در `knowledge.html`.
- آکاردئون اختصاصی باز و بسته شدن سوالات در `faq.html`.
- اسکرول نرم (Smooth Scroll) برای دکمه‌های ناوبری داخلی.

---

## ۷. استقرار در ورسل و گیت (Vercel & Git Configuration)
- **تنظیمات گیت:** ریپازیتوری اولیه با برنچ اصلی `main` و برنچ `website` به همراه `.gitignore` استاندارد.
- **تنظیمات دیپلوی ورسل (`vercel.json`):**
```json
{
  "framework": null,
  "buildCommand": "",
  "outputDirectory": ".",
  "cleanUrls": true
}
```
- **تنظیمات دسترسی بدون لاگین:** در بخش `Project Settings > Deployment Protection` ورسل، تیک `Vercel Authentication (Require Log In)` غیرفعال باشد تا کلیه لینک‌های دیپلوی برای عموم افراد بدون لاگین در دسترس باشد.
```

---

### چگونگی استفاده از این مستر پرامپت:
1. در هر ریپازیتوری یا پروژه جدیدی در ادیتور Antigravity، کل محتوای داخل باکس کد بالا را به عنوان اولین پرامپت به هوش مصنوعی ارسال کنید.
2. دستیار هوش مصنوعی با خواندن این ساختار، بی‌نیاز از توضیحات مکرر، تمامی ۱۶ صفحه، ۶ مقاله، سیستم استایل، منطق جاوااسکریپت و فایل‌های پیکربندی ورسل را با بالاترین کیفیت بازتولید می‌نماید.
